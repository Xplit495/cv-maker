RewriteEngine On

# Ne pas rediriger si le fichier ou le dossier existe
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Rediriger toutes les requêtes vers router.php
RewriteRule ^(.*)$ /web/router.php [L,QSA]
